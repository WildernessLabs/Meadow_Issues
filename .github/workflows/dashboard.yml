name: Build Dashboard

# on:
#   schedule:
#   - cron: 0 * * * *

on: workflow_dispatch
 
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Check out repository
      uses: actions/checkout@v2
    - name: 'Generate Dashboard'
      uses: ethomson/issue-dashboard@v1
      with:
        config: |
          title: 'Dashboard'
          output:
            format: markdown
            filename: 'dashboard.md'
          sections:
          sections:
          - title: 'Open Issues by Priority'
            description: 'Queries of issues by type, using their label.'
            widgets:
            - type: 'number'
              title: 'Bug'
              issue_query: 'repo:libgit2/libgit2 is:open is:issue label:"Bug - Pri-0"'
              color: 'red'
            - type: 'number'
              title: 'Feature'
              issue_query: 'repo:libgit2/libgit2 is:open is:issue label:"Bug - Pri-1"'
              color: 'green'
            - type: 'number'
              title: 'Up for Grabs'
              issue_query: 'repo:libgit2/libgit2 is:open is:issue label:"Bug - Pri-2"'
              color: 'blue'
        token: ${{ secrets.token }}

    - name: Publish Documentation
      run: |
        git add .
        git config user.name 'lamebrain'
        git config user.email 'brian.kim@wildernesslabs.co'
        git commit -m 'Documentation update' --allow-empty
        git push origin main
